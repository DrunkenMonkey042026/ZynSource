// i18n stub. Today: identity function (English-only).
// Phase 2: replace with react-i18next. All user-facing strings already wrapped in t().
export function t(key: string, _params?: Record<string, string | number>): string {
  return key
}
